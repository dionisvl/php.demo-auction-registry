all:
    hosts:
        server:
            ansible_connection: ssh
            ansible_user: root
            ansible_host: 0.0.0.0
            ansible_port: 22
            #ansible_ssh_private_key_file: ~/.ssh/Oracle cloud vm1/vm1.key